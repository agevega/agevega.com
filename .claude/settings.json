{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q 'package-lock.json' && echo 'BLOCKED: package-lock.json is a protected file. Do not modify it.' >&2 && exit 2 || exit 0"
          }
        ]
      }
    ]
  }
}
